import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lawyer.justice',
  appName: 'المحامي محمد أحمد الكامل',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
